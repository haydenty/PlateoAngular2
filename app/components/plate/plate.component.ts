import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'plate-app',
  templateUrl: 'plate.template.html',
})
export class PlateComponent  {
  //TODO: inject the comment feed component here
}
